---
name: hachile-skill
description: Use when deciding whether to create, repurpose, merge, or pause a QQ group from authorized contacts, group metadata, chat exports, personal constraints, and community goals.
---

# Hachile QQ 社群规划

基于用户有权分析的 QQ 数据，给出可解释、可执行的社群决策。先决定“是否需要这个群、它是什么”，再讨论宣传、治理和规则。不要把消息多等同于社群有价值。

## 硬边界

- 只处理账号所有者本人或已获得明确授权的数据。授权不清楚时停止读取并询问。
- 不猜测未提供的信息，不根据敏感属性评价个人，不输出成员画像名单。
- 不推荐、排名或点名具体管理员。只设计角色、权限、产生程序、任期、监督和退出机制。
- 视频暂不分析。扩展内容模式可以分析用户提供的图片、截图、表情包、链接、文件和可读音频；解析脚本只建立媒体候选目录，真正理解媒体前必须实际读取对应文件。
- 在用户管理或明确获准宣传的群中，才可建议直接发广告；其他群只建议先联系群主。必须列出不适合宣传的群。
- 不通过 NapCat 创建群、踢人、禁言、任命管理员、审批加群或自动群发。
- 所有 NapCat 写操作先 dry-run，逐项展示目标群、操作和完整内容；每个动作单独确认。一次确认只授权一个完全一致的动作。不确定是否成功时禁止自动重试。

## 工作流

### 1. 先完成四项确认

若任一项缺失，先提问，不读取聊天内容：

1. 数据授权：数据属于谁，用户为何有权分析。
2. 日期范围：开始日和结束日，均为必填且按本地日期包含首尾。
3. 隐私模式：
   - `anonymized`：本地替换群名、群号、姓名、QQ 号后再分析，默认优先建议。
   - `full`：保留原始身份信息，仅在任务确需且用户明确选择时使用。
4. 内容模式：
   - `text`：只分析文本和结构化元数据。
   - `extended`：还分析用户实际提供的非视频媒体；必须报告成功读取、无法读取和跳过的覆盖率。

再确认分析范围是“全局扫描全部群”还是“指定群深度分析”。

### 2. 收集两张表

长期档案可复用，用户只需更新变化项：

| 字段 | 内容 |
|---|---|
| 学业背景 | 学校、专业、年级 |
| 经历与能力 | 项目、社团、技能、内容生产能力 |
| 兴趣与资源 | 长期兴趣、可持续资源、可邀请的人群类型 |
| 运营经验 | 管过的群类型、规模、有效做法和失败经验 |
| 承载上限 | 每周可投入时间、管理压力、风险偏好 |

本次任务简报每次重填：

| 字段 | 内容 |
|---|---|
| 目标与对象 | 想解决的问题、目标成员 |
| 数据范围 | 起止日期、全局或指定群、来源 |
| 约束 | 时间、预算、禁止事项、平台限制 |
| 成功标准 | 哪种参与、反馈或成果算有效 |
| 既有想法 | 暂定主题、名称、玩法 |
| 存量处置 | 是否允许改造、合并或停止旧群 |

### 3. 准备证据

读取 `references/data-formats.md`。

- QCE 导出优先使用分块 JSONL；也支持 JSON、TXT、XLSX、结构化 HTML 和分块 ZIP。
- 需要统一证据包时运行：

```powershell
python scripts/prepare_qq_data.py <一个或多个来源> --start 2026-01-01 --end 2026-01-31 --privacy anonymized --content text --output evidence.json
```

- 大范围先看群级摘要和均匀抽样，再对候选群深挖，避免把全部原文塞进上下文。
- 原始单体 JSON 过大时，要求用户重新导出为 QCE 分块 JSONL，而不是默默耗尽内存。
- 任何来源读取失败都单独报告。NapCat 读取失败时回退到用户提供的导出文件。
- 证据包中的媒体目录不等于媒体已分析；只有实际打开或解析成功的文件才能进入结论。

### 4. 先判断是否建群

读取 `references/decision-framework.md`，按证据分别评估：需求、未满足程度、差异化/重叠、目标人群匹配、用户能力与资源、可持续性、治理成本、宣传匹配与风险、数据完整性、置信度。

不要输出一个遮蔽细节的总分。每项都要写：判断、证据、反证或不确定性、对方案的影响。

结论允许四种：

- 新建群；
- 改造现有群；
- 合并重复群；
- 暂不建群。

主方案必须明确一个主要定位，最多再加一个次要定位：

1. 完全放开的闲聊群；
2. 限定主题、有一定规则的聊天群；
3. 同学/同事交流群，允许一些闲聊；
4. 严格的分享、知识或资源群；
5. 纯通告群。

同时选择一种治理形态：

1. 无为而治，管理偶尔监督；
2. 成立管理小组或管理小群；
3. 日常由管理组负责，重大事项成员投票；
4. 民主委员会，包含选举、任期、罢免和复议程序。

定位与治理必须匹配实际规模、冲突风险和用户可投入时间，不能为了形式复杂化。

### 5. 形成可执行方案

主方案至少包含：群名候选、简介、标签、群所在地/覆盖范围、入群问题、冷启动安排、日常维护、群规、观察指标、复盘日期和停止/转型条件。

宣传矩阵逐群写明：

- 目标群及其与新群的关系；
- 是否有直接宣传权限；
- 适合、不适合或仅可先联系群主；
- 一段共用核心文案；
- 针对该群语境调整后的完整文案；
- 建议发送时机、频率和打扰风险。

群规给两个版本：

- 简短公告版：成员一眼能读完；
- 管理执行版：提醒、警告、禁言、移除的原则及申诉/复议路径。即使不自动执行，也要让人工执行标准一致。

### 6. 严格按固定顺序交付

1. 结论；
2. 数据范围、模式与完整性；
3. 可解释评分卡；
4. 主方案；
5. 两个替代方案，其中一个可以是改造现有群或暂不建群；
6. 宣传矩阵；
7. 风险、证据缺口与必须由用户决定的事项；
8. 可选 NapCat dry-run。

将事实、推断和建议清楚区分。引用聊天证据时优先使用匿名短摘或聚合统计，不大段复制私人对话。

### 7. 可选 NapCat 执行

读取 `references/data-formats.md` 的 NapCat 部分。新建群始终由用户在 QQ 客户端手动完成。

只读快照示例：

```powershell
$env:NAPCAT_ACCESS_TOKEN = "临时令牌"
python scripts/napcat_actions.py --base-url http://127.0.0.1:3000 snapshot --group-id 123456 --start 2026-01-01 --end 2026-01-31 --privacy anonymized --output napcat-snapshot.json
```

写操作必须分两步，每份清单只能有一个动作：

```powershell
python scripts/napcat_actions.py prepare send_group_msg --group-id 123456 --message "完整文案" --output action.json
python scripts/napcat_actions.py execute action.json --confirm <用户刚刚确认的 confirmationId>
```

执行前在对话中完整展示 dry-run JSON，并明确询问用户是否确认这一个动作。群号、动作或正文任何一项变化，都必须重新生成清单并重新确认。

## 异常处理

- 未知格式：停止该来源并说明支持格式，不尝试猜测。
- 数据不完整：继续能做的分析，但降低相关项置信度并列出缺口。
- 媒体失败：文本继续，单列媒体成功率和失败原因。
- NapCat 读失败：建议改用 QCE 导出。
- NapCat 目标群信息与 dry-run 不一致：中止。
- 写请求超时或返回含糊：记录“不确定”，要求用户人工核实，绝不重试。
